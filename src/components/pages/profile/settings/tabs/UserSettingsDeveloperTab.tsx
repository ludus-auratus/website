import { TabsContent } from "@/components/ui/tabs";

export function UserSettingsDeveloperTab({ tabId }: { tabId: string }) {
  return (
    <TabsContent value={tabId} className="space-y-6">
      a
    </TabsContent>
  );
}
